name: CI

# Test gate: runs on every PR into main and on main itself. Cloudflare Workers
# Builds handles deployment; this only validates. No credentials used here.
# Pair with a branch-protection rule requiring this check before merging to main.
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

# Least privilege: CI only needs to read the repo.
permissions:
  contents: read

# Cancel superseded runs on the same ref to save minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Typecheck · test · build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      # cloudflare-env.d.ts is committed (it types secrets from .dev.vars, which
      # is absent in CI) — do NOT regenerate it here or those types are lost.
      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      # Full OpenNext production build — catches build-only breakage. No deploy,
      # no credentials.
      - name: Build (OpenNext)
        run: npx opennextjs-cloudflare build
